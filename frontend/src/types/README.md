This folder is reserved for future shared data contracts.

For the prototype build, the data shapes are kept in the mock and service layers so the UI can be built quickly.
In the next phase, these structures can be converted into dedicated TypeScript interfaces or validation schemas.
