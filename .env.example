# .env.example — template for Docker Compose credentials
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# .env is gitignored — this example file is safe to commit.

DB_USER=admin
DB_PASSWORD=your_password_here
DB_NAME=capstone_db
